

# 纽约出租车数据智能分析系统 (NYC Taxi Intelligence)

## 项目简介

本项目基于 2023 年 1 月纽约市真实出行数据，通过人机协作（Vibe Coding）模式，构建了一个包含数据自动化清洗、神经网络需求预测及 LLM 智能交互功能的完整数据科学系统。

---

## 核心报告：人机协作开发日志 

### 1. AI 交互日志：关键决策与容错记录

本项目的开发过程记录了从初创到填坑的全过程，重点展示了对 AI 输出的甄别与决策：

* M1 & M2 (数据清洗与可视化):
* Prompt: "帮我处理 2023 年 1 月纽约出租车数据，要求过滤无效单量、清洗地理区域（剔除 264, 265），并生成 24 小时需求量折线图。"
* AI 输出摘要: 提供了基于 pandas 和 pyarrow 的加载方案，并使用 seaborn 绘图。
* 决策: 采用并微调。AI 初始代码未区分工作日，我决策要求加入 day_of_week 特征，实现了工作日与周末的对比分析。


* M3 (神经网络预测) :
* AI 犯错点: AI 最初强烈坚持使用 TensorFlow 构建 Keras 模型。
* 发现问题: 在执行安装时，系统反复报 "No matching distribution found"。经核实，这是因为本地 Python 3.13 版本过新，而 TensorFlow 对该版本的支持存在滞后。
* 修正决策: 我反馈报错截图后，拒绝了 AI 尝试通过修改底层环境来适配的建议。我主动引导 AI 切换到更轻量、兼容性最强的 Scikit-learn 里的 MLPRegressor。
* 结果: 仅用 30 行代码便实现了带 Loss 曲线记录的神经网络，避开了无限的环境配置陷阱。


* M4 (LLM 问答系统):
* 决策: 修改了 AI 提供的 Prompt 模板，加入了“引导用户查看 output 图表”的逻辑，使系统具备了更强的任务闭环能力。



### 2. 三阶段对比：以“需求预测功能”为例

通过对核心预测模块的演进对比，分析 AI 辅助开发的深度：

| 版本 | 代码实现逻辑 | 开发效率 | 理解深度 |
| --- | --- | --- | --- |
| Native 版 | 使用简单的 LinearRegression。 | 低 (需查阅大量文档) | 仅能处理线性关系，无法捕捉交通流的非线性波动。 |
| Prompt 版 | 试图使用 tf.keras.Sequential。 | 极低 (环境崩溃) | 追求技术“大杀器”，却忽视了工程落地的环境约束。 |
| Vibe 版 | (最终方案) MLPRegressor + StandardScaler。 | 极高 (对话即所得) | 深刻理解了在特定版本约束下选择“最优解”而非“最贵解”的工程思维。 |

### 3. 反思：对 AI 工具能力边界的新认识

通过本次作业，我认识到 AI 工具的上限取决于人类的“批判性思维”：

1. AI 的“知识滞后”瓶颈：AI 在处理最新的软件版本（如 Python 3.13）或特定的网络冲突（如 Git 443 推送错误）时，往往会给出过时的建议。开发者必须具备“喊停”的能力。
2. “Prompt”不如“Context”：好的代码不是一次性生成的，而是在不断反馈报错截图、不断校准目标的过程中磨合出来的。
3. 未来的核心竞争力：不是写出代码，而是在 AI 提供的多条路径中，根据实际的硬件、网络和项目周期，选出最稳健的那一条。

---

## 项目结构说明

* data/: 原始数据存放目录
* outputs/: 自动化生成的分析图表 (m2_*.png, m3_loss_curve.png)
* main.py: 项目主程序 (包含 M1-M4 全部逻辑)
* README.md: 包含开发报告的项目说明

## 技术栈与环境

* 语言: Python 3.13
* 机器学习库: Scikit-learn (MLP 神经网络)
* 数据处理: Pandas, Pyarrow
* 大模型驱动: DeepSeek-V3 API
* 版本管理: Git (已解决 master/main 分支迁移冲突)

## 快速开始

1. 在 main.py 中填入您的 DeepSeek_API_KEY。
2. 执行程序：python main.py
3. 查看 outputs/ 文件夹获取分析结果。
